spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/garage_db
    username: garage
    password: garage
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
      show_sql: true
server:
  port: 8081
